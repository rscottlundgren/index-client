<!-- Iterates Through Characters JSON Object, Running Each Individual --------->
<!-- Character Through The Template ------------------------------------------->
{{#each characters as |character|}}
<!-- Add Data-ID To The Card For Iteration Purposes, Allows Information ------->
<!-- Population, & Reference As A Event Item ---------------------------------->
<div class="card bg-transparent" data-id="{{character.id}}">
  <div class="card-header" id="heading-{{character.id}}">
    <h2 class="mb-0">
      <button class="btn btn-link bg-transparent" type="button"
      data-toggle="collapse" data-target="#collapse-{{character.id}}"
      aria-expanded="true" aria-controls="collapse-{{character.id}}">
        {{character.char_name}} | {{character.char_class}} | {{character.char_level}}
      </button>
    </h2>
  </div>
  <div id="collapse-{{character.id}}" class="collapse show"
  aria-labelledby="heading-{{character.id}}" data-parent="#char-list-accord">
    <div class="card-body">
      Mommy Wow! I Have Handlebars Now!
    </div>
  </div>
</div>
<!-- Closing Handlebars Tag --------------------------------------------------->
{{/each}}
